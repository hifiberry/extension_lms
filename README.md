# Logitech Media Server extension for HiFiBerryOS

Provides a Logitech media server. Web interface runs on HTTP port 9000. The music library from the host system is available in the directory /library.
